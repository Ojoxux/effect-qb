# Contributing

## Workflow

- Keep changes focused and small.
- Run `bun test`, `bun run test:types`, and the package build before opening a PR.
- Update docs and changelog entries together with behavior changes.
- Do not commit generated `dist` artifacts.

## Releases

- Release commits are created by `bun run release`.
- Keep release-related changes isolated from feature work when possible.
